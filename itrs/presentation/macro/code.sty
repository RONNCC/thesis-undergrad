\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{macro/code}
  [2016/01/29 version 1.0.0 Code]

\RequirePackage{listings}
\RequirePackage{color}

% Types
\newcommand\nat{\mathtt{Nat}}
\newcommand\pos{\mathtt{Pos}}
\newcommand\even{\mathtt{Even}}
\newcommand\odd{\mathtt{Odd}}

% Colors
\definecolor{krill-green}{RGB}{0, 132, 61}
\definecolor{krill-yellow}{RGB}{180, 125, 0}
\definecolor{krill-red}{RGB}{180, 0, 0}
\definecolor{krill-blue}{RGB}{0, 0, 180}
\definecolor{krill-gray}{rgb}{0.5,0.5,0.5}

% Listings settings
\lstdefinelanguage{krill}{%
  % Symbols
  keywords={},
  otherkeywords={*,+,-o,&},
  keywordstyle=\color{krill-red},
  % types
  keywordstyle=[2]\color{krill-blue},
  morekeywords=[2]{1, type, data, and, or},
  % Base keywords
  keywordstyle=[3]\color{krill-green},
  morekeywords=[3]{},
  % Concurrent keywords
  keywordstyle=[4]\color{krill-yellow},
  morekeywords=[4]{.,close, wait, send, recv, case, of},
  % Language
  sensitive=true,
  morecomment=[l]{--},
  morecomment=[n]{\{-}{-\}},
  % Comments
  mathescape=true,         % Latex math mode using $...$
  escapechar=@,            % Latex inside code with @...@
  escapeinside={\{*}{*\}}, % Latex comment with {* ... *}
}

\lstset{%
  language=krill,
  basicstyle=\ttfamily\small,
  commentstyle=\itshape\color{krill-gray},
  %
  breakatwhitespace=false,         % sets if automatic breaks should only happen at whitespace
  breaklines=true,                 % sets automatic line breaking
  captionpos=b,                    % sets the caption-position to bottom
  frame=none,                      % adds a frame around the code
  keepspaces=true,                 % keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
  numbers=none,                    % where to put the line-numbers; possible values are (none, left, right)
  numbersep=5pt,                   % how far the line-numbers are from the code
  numberstyle=\tiny\color{gray},   % the style that is used for the line-numbers
  stepnumber=2,                    % the step between two line-numbers. If it's 1, each line will be numbered
  rulecolor=\color{black},         % if not set, the frame-color may be changed on line-breaks within not-black text
  tabsize=2,                       % sets default tabsize to 2 spaces
}

